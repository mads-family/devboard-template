# Porta onde a API vai rodar
PORT=3000

# Configurações do banco PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=devboard_db

# Tempo de expiração do token JWT (se usar autenticação)
# JWT_EXPIRATION=3600

# Variáveis extras podem ser adicionadas conforme necessidade
